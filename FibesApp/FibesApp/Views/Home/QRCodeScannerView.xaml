<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Mobile.Forms;assembly=ZXing.Net.Mobile.Forms"
             x:Class="FibesApp.Views.Home.QRCodeScannerView" NavigationPage.HasNavigationBar="True">
      
    <Grid VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
        <zxing:ZXingScannerView x:Name="zxingScanner"                                   
                    IsScanning="True" 
                    IsAnalyzing="True" 
                    OnScanResult="QRCodeResult" />
        <zxing:ZXingDefaultOverlay               
                x:Name="scannerOverlay"                                                       
                BottomText="Place the red line over the barcode you'd like to scan." />

        <Grid HorizontalOptions="StartAndExpand" VerticalOptions="StartAndExpand" HeightRequest="50" WidthRequest="80" 
              BackgroundColor="Transparent">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Tapped="CancelTabbed"/>
            </Grid.GestureRecognizers>
            <Label HorizontalOptions="CenterAndExpand" HorizontalTextAlignment="Center" VerticalOptions="Center"
                   Text="Cancel" FontAttributes="Bold" TextColor="White"/>
        </Grid>

    </Grid>

</ContentPage>